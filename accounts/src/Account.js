function Account(username, password) {
    this.username = new Username(username);
    this.password = new Password(password);
}

Account.prototype.getUsername = (username) => {
     
};

Account.prototype.setUsername = () => {
    
};

Account.prototype.setPassword = () => {
    
};

Account.prototype.validatePassword = () => {
    
};

